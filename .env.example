# Meta Ads MCP Server Configuration

# Meta access token (fallback for single-tenant / local dev)
# For multi-tenant, clients send tokens via Authorization: Bearer <token>
META_ACCESS_TOKEN=

# Meta Graph API version
META_API_VERSION=v22.0

# Server port
PORT=3000

# Log level: debug | info | warn | error
LOG_LEVEL=info

# Node environment
NODE_ENV=production

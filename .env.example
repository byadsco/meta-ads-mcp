# Meta Ads MCP Server Configuration

# Meta access token (required — used for all Meta API calls)
META_ACCESS_TOKEN=

# OAuth JWT signing secret (required in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
OAUTH_SECRET=

# SECURITY: Without a PIN, anyone who can reach this server can approve OAuth access.
# Set a strong random PIN in production.
# Generate with: node -e "console.log(require('crypto').randomBytes(8).toString('hex'))"
OAUTH_APPROVAL_PIN=

# Public URL of this server (required in production for OAuth discovery)
# e.g. https://meta-ads-mcp-xxx.run.app
SERVER_URL=

# Meta Graph API version
META_API_VERSION=v22.0

# Server port
PORT=3000

# Log level: debug | info | warn | error
LOG_LEVEL=info

# Node environment
NODE_ENV=production
